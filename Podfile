platform :ios, '9.0'

target 'example-ios-buddybuild' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for example-ios-buddybuild
  pod 'SwiftHTTP', '~> 2.0'
  pod 'PaginatedScrollView', '~> 0.2'
  pod 'ContactCircularView', '~> 1.1'
  pod 'Compose', '~> 1.2'
  pod 'expanding-collection', '~> 1.1'

  target 'example-ios-buddybuildTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'example-ios-buddybuildUITests' do
    inherit! :search_paths
    # Pods for testing
  end

end
